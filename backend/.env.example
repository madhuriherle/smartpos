DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/inventory_db
CORS_ORIGINS=http://localhost:5174
SECRET_KEY=change-me-generate-a-real-random-value
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me-set-a-real-password
COOKIE_SECURE=false

# Optional — leave blank to disable email sending
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
